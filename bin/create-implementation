#!/bin/sh
set -euf
top="$(git rev-parse --show-toplevel)"

mkdir_with_ai() {
  dir="$1"
  mkdir "$1" && cd $_
  touch index.md
  ln -sfn index.md README.md
  touch AGENTS.md
  echo "@AGENTS.md" > CLAUDE.md
  touch plan.md
  touch tasks.md
}

mkdir_with_ai "$top/$1"
